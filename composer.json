{
    "name": "stefan-bed/przypominanie",
    "description": "Aplikacja PHP do przypominania o nierozliczonych dokumentach",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if(!is_dir('public')) mkdir('public', 0775, true); if(!is_dir('src')) mkdir('src', 0775, true); if(!is_dir('storage')) mkdir('storage', 0770, true);\""
        ],
        "post-update-cmd": [
            "@php -r \"if(!is_dir('public')) mkdir('public', 0775, true); if(!is_dir('src')) mkdir('src', 0775, true); if(!is_dir('storage')) mkdir('storage', 0770, true);\""
        ]
    }
}
